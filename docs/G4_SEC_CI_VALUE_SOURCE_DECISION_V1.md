# G4 SEC-CI Value Source Decision V1

## Current G4 state

- `NO_PLAINTEXT_SCAN_RESULT`: `PASS`
- `SEC-CI` storage target present: `NO`
- `SEC-CI` value source confirmed: `NO`
- provisioning result: `BLOCKED`
- first real `SEC-CI` rehearsal may start: `NO`

Current fixed completed gates:

- `G1 — Deployment and rollback drills pass`: `DONE`
- `G2 — P0/P1 alerting verified`: `DONE`
- `G3 — Backup/restore drill within RPO/RTO`: `DONE`

Current still-forbidden boundaries:

- go-live: `NO-GO`
- real external request: `NOT AUTHORIZED`
- live trading: `NO-GO`
- production overwrite: `NO`

## Decision needed

The project still needs one explicit decision before `SEC-CI` provisioning may move forward:

- identify the authoritative source of the real `SEC-CI` value
- identify the human operator responsible for provisioning it
- define whether `SEC-CI` is generated, received, or retrieved from an already approved system
- define the non-disclosure rule for the value itself

Decision result at this stage:

- authoritative value source selected: `NO`
- responsible human operator role: `Release manager`
- concrete operator confirmed: `NO`

Allowed value-source classes for future confirmation:

1. generated by the responsible operator specifically for this bounded rehearsal
2. received through an approved maintainer or release-manager handoff
3. retrieved from an already approved secret-management system

Disallowed handling:

- storing the real value in git-tracked files
- storing the real value in docs
- printing the real value into shell output
- pasting the real value into chat
- copying the real value into logs or evidence records

The value must only be used in a future bounded operator provisioning step aimed at the already-decided GitHub repository Actions secret target.

## Mandatory stop conditions

Provisioning must remain `BLOCKED` if any of the following are true:

- `SEC-CI` value source is still unknown
- the responsible operator cannot verify the source
- any step would print or disclose the value
- rehearsal would execute before presence-only verification completes

## Next allowed step after this decision

Once the value source is actually confirmed, the next allowed step is still narrow:

1. operator manual provisioning only:
   - `gh secret set SEC_CI --app actions`
2. presence-only verification only:
   - verify the repository Actions secret target exists without exposing the value
3. no rehearsal yet:
   - no real rehearsal until `SEC_CI_STORAGE_TARGET_PRESENT=YES`

## Explicit non-claims

- this decision does not create `SEC-CI`
- this decision does not execute rehearsal
- this decision does not complete `G4`
- this decision does not authorize go-live
- this decision does not authorize live trading
- this decision does not authorize real external requests

## Final decision result

- `SEC-CI` value source decision prepared: `YES`
- `SEC-CI` value source confirmed: `NO`
- `SEC-CI` storage target present: `NO`
- provisioning result: `BLOCKED`
- first real `SEC-CI` rehearsal may start: `NO`
