# Parent repo (local_box control plane + risk_engine HTTP client).
# Install: python3 -m pip install -r requirements.txt
#
# SQLite for local_box audit: default <repo-root>/anchor.db, or set LOCAL_BOX_DB_PATH.
requests>=2.26,<3
flask>=3.1.3,<4
